# Matrix inverse update

Updates a known matrix inverse after replacing one column of the original matrix. This operation is reused when the simplex basis changes.

Run the example from the repository root:

```sh
python matrix_inversion/main.py
```

The algorithm is in [`core.py`](core.py); [`main.py`](main.py) contains the runnable assignment example.
